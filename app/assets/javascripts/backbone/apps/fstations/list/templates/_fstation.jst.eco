<%= _.map @properties, (value, key) => %>
		<%= _.map value, (value, key) => %>
			<%= _.pluck value, 'name', (name) => %>
				<% for each in name: %>
					<td><%= "#{name}"%></td>
		<% end %>
<% end %>
